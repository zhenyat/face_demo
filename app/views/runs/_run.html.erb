<table class='table table-striped'>
  <tbody>
    <% @run.attributes.each do |key, value| %>
      <tr>
        <% unless key == 'id' || key.include?('digest') || key == 'created_at' || key == 'updated_at' ||  key == 'cover_image_url'  %>
          <% if key == 'status' %>
            <td><%= t 'status.status' %></td>
            <td><%= status_mark @run.status %></td>
          <% else %>
            <td><%= t "activerecord.attributes.run.#{key}" %></td>
            <td><%= value %></td>
          <% end %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>